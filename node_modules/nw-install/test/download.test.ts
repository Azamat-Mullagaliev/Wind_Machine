test("if arguments are valid, download function works", async () => {
    const download = jest.fn();
    download.mockResolvedValue(0);
    expect(download("0.67.1", "sdk", "linux", "x64", "https://nwjs.io/", "https://dl.nwjs.io", "./cache", "nw_67_sdk_nix_64")).resolves.toBe(0);
});

export {};